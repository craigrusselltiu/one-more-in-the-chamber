ok, when deadeye activates, the player gets 3 shots. in deadeye mode, the next 3 tiles the user clicks gets
  triggered & cleared. for each tile, it must wait for cascading to finish before being able to click the
  next. after 3 shots, deadeye mode completes, and the user goes back to normal mode, and ability charge
  resets to 0. deadeye clicks do NOT use swaps.