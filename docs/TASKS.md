issues:
- deadeye doesn't work, make it so that pressing space activates your ability if ready
- in the console, if a cascade has two 3-match bullets it seems to appear as a 6-match. it should be two separate 3-matches. also I see sometimes 1-match or 2-match. how is that possible?
- if an enemy is about to do multiple things in a turn, e.g. ATK and BLK, it should show all like "ATK 5, BLK 10", or "HEX 1, CALL 1, ATK 10"
- dusty_dan should do the board gravity change as his first move always.
- fix the errors and warnings in the console.

adjustments:
- make game speed slower, and add option to adjust gamespeed 1x, 2x, and 3x this in settings.
- make swap animation slower so you can see what is being swapped.
- if player performs a swap, even if it is invalid, it should show the swapping animation as if it attempted to swap, then swap back to what it was before. make sure you can't swap other tiles while this is happening.
- make settings a popup instead of a drop-down now to handle above. the give up button should be moved to the new popup.
- add master volume adjuster in settings.